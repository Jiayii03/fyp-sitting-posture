## FYP Sitting Posture - Backend

### Quickstart

1. Run `docker-compose up -d` to start Kafka broker. Use `docker ps` to verify if they are running.To stop and remove containers, use `docker-compose down -v`.